# 快速开始(新电脑 + 已刷固件的 Passport)

目标:插上/连上就能用 → **UP 说话、任意键结束、DOWN 粘贴、OK 发送**。

## 一次性准备(约 10 分钟,只做一次)

| # | 做什么 | 说明 |
|---|---|---|
| 1 | 装 **Python 3.10+** | 安装时勾选 *Add python.exe to PATH* |
| 2 | 装 **VB-CABLE** 虚拟声卡 | vb-audio.com/Cable 下载,VBCABLE_Setup_x64.exe **右键以管理员身份运行**,装完重启一次电脑 |
| 3 | 把本项目拷到电脑,双击 **`install.cmd`** | 自动建 `.venv` + 装依赖 + 生成配置 |
| 4 | 双击 **`doctor.cmd`** 体检 | 全部 `[OK]`,或只剩设备未连接 → 可以继续 |

`doctor.cmd` 是唯一需要记的自检入口:缺什么、缺了装什么,它直接告诉你。

## 每次使用

1. 双击 **`open-console.cmd`** → 浏览器打开控制台 `http://127.0.0.1:8790/`
2. 连接配置里选通道:
   - **USB**:插 USB 线(有线,最稳,任何电脑都能用)
   - **蓝牙 BLE**:不插线(需要电脑有蓝牙;首次连接会自动配对)
3. 点 **保存并重连**,右上角变成 `ONLINE` 就绪
   (之后不用再点:设备没开机/走远了,桥接会自动退避重连,设备回来就自动接上;换通道时才需要再点一次)
4. 设备上操作:
   - 单击 **UP** → 开始语音输入(设备屏幕上出现橙色电平条)
   - 说话
   - 说话中**按任意键** → 结束语音,文本进微信输入法并复制
   - 单击 **DOWN** → 粘贴到当前输入框
   - 单击 **OK** → 回车发送

微信输入法不用手动改麦克风:桥接会在你按 UP 的瞬间把**默认麦克风临时切到 CABLE Output**,说话结束立刻还原给你自己的耳机/耳麦(想关掉这个行为,配置里 `mic_auto_switch=false`)。

## 出问题按这个顺序查

1. 双击 **`doctor.cmd`** —— 哪项是 `[XX]` 就先修哪项
2. 控制台右下 **运行日志** 看 Bridge 的报错原文
3. 常见三种:
   - `[XX] 虚拟声卡` → VB-CABLE 没装/装完没重启
   - `[!!] 设备/BLE` → 蓝牙被禁用,或系统提示需要重启 → 重启电脑
   - 设备按了没反应 → 控制台看 `phase` 是不是 `connected`;不是就点保存并重连

## 为什么必须有电脑端

| | 角色 | 干什么 |
|---|---|---|
| Passport | 麦克风 + 三键遥控器 | 采集音频、发按键事件(USB 或 BLE 上行) |
| 电脑上的 Bridge | 接线员 + 打字员 | ① 把音频写进虚拟声卡,让输入法以为麦克风在说话;② 真的按 Ctrl+Win+Shift / Ctrl+V / Enter |

设备端只烧了按键策略;所有和输入法/系统对接的事都在电脑上。所以设备单独跑是没用的。

## 进阶(可选)

- 开机自启:把 `open-console.cmd` 的快捷方式放进 `shell:startup`(`Win+R` 输入 `shell:startup`),开机后控制台在后台跑,设备连上会自动重连。
- 有线/无线随时切:控制台切通道 → 保存并重连;桥接断线会自动重连(2s 退避)。
- 固件重刷:`build-firmware.cmd` 编译,`tools/passport_flash.py` 只写 `0x10000` 应用分区(不动分区表/NVS/cardid/recovery)。
- 重连策略:`companion/wechat_config.json` 里 `connect_retries` 默认 `0` = 无限重试(退避 2→30s);
  想让它失败就退出,设成正整数(例如 `5`)。
