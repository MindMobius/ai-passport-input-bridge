# 统一行尾:源码一律 LF(WSL 里的 bash / gcc 需要),Windows 批处理保持 CRLF
* text=auto eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# 二进制(固件/图片/音频)不做行尾转换
*.bin binary
*.png binary
*.jpg binary
*.wav binary
*.pdf binary
